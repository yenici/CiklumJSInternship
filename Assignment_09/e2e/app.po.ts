import { browser, element, by } from 'protractor';

export class Assignment09aPage {
  navigateTo() {
    return browser.get('/');
  }

  getParagraphText() {
    return element(by.css('app-root h1')).getText();
  }
}
